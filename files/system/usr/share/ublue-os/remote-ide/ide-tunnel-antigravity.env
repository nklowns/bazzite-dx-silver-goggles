# aguardando fix upstream: `antigravity-ide tunnel` falha com
# `spawn .../bin/antigravity-tunnel ENOENT` — o binário do tunnel não é
# empacotado no cask/tarball atual (só chat e serve-web funcionam parcialmente).
# Quando corrigido upstream, `ujust remote-ide-tunnel antigravity` passa a funcionar.
IDE_BINARY=antigravity-ide
TUNNEL_NAME=bazzite-antigravity

# Bazzite-DX Specialized Kernel Arguments
# Focused on Virtualization performance and IOMMU for DX workloads.

kargs = [
    "iommu=pt",
    "kvm.ignore_msrs=1",
    "kvm.report_ignored_msrs=0",
    "rd.driver.pre=vfio-pci",
    "vfio_pci.disable_vga=1",
    "kvm.nested=1"
]

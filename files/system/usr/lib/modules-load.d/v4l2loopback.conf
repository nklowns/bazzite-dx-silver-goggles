# Load proactively at boot so this image's device pool (see
# /etc/modprobe.d/v4l2loopback.conf) wins the module-parameter race against whichever app
# (OBS Studio's Virtual Camera, webcam-receiver) would otherwise trigger a lazy modprobe with
# its own conflicting options first.
v4l2loopback

cosign.key
_build_*
_build-*/**
# Ignore all RPMs by default (build artifacts)
*.rpm
# Exception: the pre-compiled AWCC RPM is intentionally committed as the
# canonical binary for the image build. Rebuilt via: just dev-awcc-rpm <src>
!files/awcc-dev.rpm
output/
*.qcow2
*.raw
*.iso
/.bluebuild*/
/.bluebuild-scripts_*
